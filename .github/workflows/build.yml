name: Build

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest, ubuntu-latest]

    runs-on: ${{ matrix.os }}

    steps:
    - uses: actions/checkout@v4
      with:
        submodules: recursive

    - name: Install Linux build dependencies
      if: runner.os == 'Linux'
      run: |
        sudo apt-get update
        sudo apt-get install -y ninja-build pkg-config \
          libx11-dev libxft-dev libxext-dev libwayland-dev libxkbcommon-dev \
          libxrandr-dev libxinerama-dev libxcursor-dev libxi-dev libgl1-mesa-dev

    - name: Setup MSVC environment
      if: runner.os == 'Windows'
      uses: ilammy/msvc-dev-cmd@v1

    - name: Bootstrap vcpkg (Linux)
      if: runner.os == 'Linux'
      run: ./vcpkg/bootstrap-vcpkg.sh -disableMetrics

    - name: Bootstrap vcpkg (Windows)
      if: runner.os == 'Windows'
      run: .\vcpkg\bootstrap-vcpkg.bat -disableMetrics

    - name: Check the compiler is MSVC
      if: runner.os == 'Windows'
      shell: cmd
      run: |
        where cl.exe || (echo MSVC not on PATH && exit /b 1)

    - name: Configure (Windows)
      if: runner.os == 'Windows'
      run: cmake --preset release -DVCPKG_TARGET_TRIPLET=x64-windows-static-md

    - name: Configure (Linux)
      if: runner.os == 'Linux'
      run: cmake --preset release

    - name: Build
      run: cmake --build --preset release

    - name: Upload CLI artifact
      uses: actions/upload-artifact@v4
      with:
        name: a3texture-cli-${{ runner.os }}
        path: |
          build/release/a3texture-cli
          build/release/a3texture-cli.exe
        if-no-files-found: ignore

    - name: Upload GUI artifact
      uses: actions/upload-artifact@v4
      with:
        name: a3texture-gui-${{ runner.os }}
        path: |
          build/release/a3texture-gui
          build/release/a3texture-gui.exe
        if-no-files-found: ignore

  release:
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    needs: [build]
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
    - name: Download Windows CLI
      uses: actions/download-artifact@v4
      with:
        name: a3texture-cli-Windows
        path: windows-cli

    - name: Download Windows GUI
      uses: actions/download-artifact@v4
      with:
        name: a3texture-gui-Windows
        path: windows-gui

    - name: Download Linux CLI
      uses: actions/download-artifact@v4
      with:
        name: a3texture-cli-Linux
        path: linux-cli

    - name: Download Linux GUI
      uses: actions/download-artifact@v4
      with:
        name: a3texture-gui-Linux
        path: linux-gui

    - name: Create Release
      uses: softprops/action-gh-release@v1
      with:
        tag_name: v${{ github.run_number }}
        name: Build ${{ github.run_number }}
        draft: false
        prerelease: false
        files: |
          windows-cli/a3texture-cli.exe
          windows-gui/a3texture-gui.exe
          linux-cli/a3texture-cli
          linux-gui/a3texture-gui
